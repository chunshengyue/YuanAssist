这份附件主要是源码，所以我把项目里的环境依赖去掉了，原来的包有一个多G。

如果要运行 Python_Table_OCR：

先进入 Python_Table_OCR 目录，然后创建虚拟环境，激活虚拟环境，再安装 requirements.txt 里的依赖。

常见命令如下：

cd Python_Table_OCR
python -m venv .venv
.venv\\Scripts\\Activate.ps1
pip install -r requirements.txt

附件没有完整的 Paddle OCR 运行资源，需要额外下载。
